# Time Tracker License Server - Mimari Topoloji

## 🏗️ Sistem Mimarisi

### Genel Bakış

Bu dokümantasyon, Time Tracker Azure DevOps Extension için lisans yönetim sisteminin mimari topolojisini detaylı olarak açıklamaktadır.

---

## 📐 Mimari Diyagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        EXTERNAL LAYER                                        │
│                                                                              │
│  ┌────────────────────┐              ┌────────────────────┐                │
│  │ Azure DevOps Cloud │              │ Azure DevOps Server│                │
│  │ (dev.azure.com)    │              │ (Self-hosted)      │                │
│  │                    │              │                    │                │
│  │ Organization: org1 │              │ Host: host1.com    │                │
│  └─────────┬──────────┘              └─────────┬──────────┘                │
└────────────┼────────────────────────────────────┼───────────────────────────┘
             │                                    │
             │ Extension Installed                │ Extension Installed
             │                                    │
┌────────────▼────────────────────────────────────▼───────────────────────────┐
│                      FRONTEND LAYER (Extension)                              │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                    GOTime Extension                                  │  │
│  │                    (TypeScript + React)                              │  │
│  │                                                                      │  │
│  │  ┌──────────────────────────────────────────────────────────────┐  │  │
│  │  │  Components:                                                  │  │  │
│  │  │  • effort-activity (Calendar View)                           │  │  │
│  │  │  • effort-report (Reports)                                   │  │  │
│  │  │  • effort-settings (Settings + License)                      │  │  │
│  │  │  • effort-dialog (Time Entry)                                │  │  │
│  │  │  • effort-workitem (Work Item Integration)                   │  │  │
│  │  └──────────────────────────────────────────────────────────────┘  │  │
│  │                                                                      │  │
│  │  ┌──────────────────────────────────────────────────────────────┐  │  │
│  │  │  Services:                                                    │  │  │
│  │  │  • LicenseManager (license-utils.ts)                         │  │  │
│  │  │    - RSA signature verification                              │  │  │
│  │  │    - Organization/Host validation                            │  │  │
│  │  │    - Expiration checking                                     │  │  │
│  │  │    - License storage (Azure DevOps Settings)                 │  │  │
│  │  │  • SettingsStorage                                           │  │  │
│  │  │  • EffortStore                                               │  │  │
│  │  └──────────────────────────────────────────────────────────────┘  │  │
│  │                                                                      │  │
│  │  ┌──────────────────────────────────────────────────────────────┐  │  │
│  │  │  Storage:                                                     │  │  │
│  │  │  • Azure DevOps Collection Settings (licenseInfo)            │  │  │
│  │  │  • IndexedDB (time logs, effort data)                        │  │  │
│  │  └──────────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────┬──────────────────────────────────────────────┘  │
└───────────────────────────┼──────────────────────────────────────────────────┘
                            │
                            │ HTTPS API Calls
                            │ POST /api/license/demo
                            │ POST /api/license/generate
                            │ POST /api/license/validate
                            │
┌───────────────────────────▼──────────────────────────────────────────────────┐
│                      BACKEND LAYER (License Server)                           │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                  Express.js Server                                  │   │
│  │                  Port: 3001                                         │   │
│  │                  Environment: Node.js 20                            │   │
│  │                                                                      │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │  Routes (/api/license)                                        │  │   │
│  │  │  • POST /demo                                                 │  │   │
│  │  │    - Demo license generation (3 months, 100 users)            │  │   │
│  │  │    - Organization/Host conflict checking                      │  │   │
│  │  │    - Database storage                                         │  │   │
│  │  │  • POST /generate                                             │  │   │
│  │  │    - Custom license generation                                │  │   │
│  │  │  • POST /validate                                             │  │   │
│  │  │    - License validation (server-side)                         │  │   │
│  │  └──────────────────────────────────────────────────────────────┘  │   │
│  │                                                                      │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │  Health Checks                                                │  │   │
│  │  │  • GET /healthz (basic health)                               │  │   │
│  │  │  • GET /readyz (database readiness)                          │  │   │
│  │  └──────────────────────────────────────────────────────────────┘  │   │
│  │                                                                      │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │  License Generator Core                                       │  │   │
│  │  │  (tools/license-generator-core.js)                           │  │   │
│  │  │  • RSA Private Key (signing)                                 │  │   │
│  │  │  • RSA Public Key (verification)                             │  │   │
│  │  │  • node-forge library                                        │  │   │
│  │  │  • SHA-256 hashing                                           │  │   │
│  │  │  • Base64 encoding                                           │  │   │
│  │  └──────────────────────────────────────────────────────────────┘  │   │
│  └───────────────────────────┬──────────────────────────────────────────┘   │
└───────────────────────────────┼──────────────────────────────────────────────┘
                                │
                                │ Database Queries
                                │
┌───────────────────────────────▼──────────────────────────────────────────────┐
│                        DATABASE LAYER                                         │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Database Abstraction (db.js)                                       │   │
│  │  • MySQL (Production)                                               │   │
│  │  • SQLite (Development)                                             │   │
│  │  • Connection pooling (MySQL)                                       │   │
│  └───────────────────────────┬─────────────────────────────────────────┘   │
│                              │                                               │
│  ┌───────────────────────────▼─────────────────────────────────────────┐   │
│  │  Database Schema                                                    │   │
│  │                                                                      │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │  demo_licenses                                                │  │   │
│  │  │  • id (PK)                                                    │  │   │
│  │  │  • identifier (UNIQUE) - org|host key                         │  │   │
│  │  │  • license_key (TEXT) - Generated license                     │  │   │
│  │  │  • payload (TEXT) - JSON payload                              │  │   │
│  │  │  • created_at (TIMESTAMP)                                     │  │   │
│  │  └──────────────────────────────────────────────────────────────┘  │   │
│  │                                                                      │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │  demo_license_organizations                                   │  │   │
│  │  │  • id (PK)                                                    │  │   │
│  │  │  • license_id (FK → demo_licenses.id)                        │  │   │
│  │  │  • organization (UNIQUE) - Azure DevOps org name              │  │   │
│  │  │  • INDEX: idx_demo_license_org_license                        │  │   │
│  │  └──────────────────────────────────────────────────────────────┘  │   │
│  │                                                                      │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │  demo_license_hosts                                           │  │   │
│  │  │  • id (PK)                                                    │  │   │
│  │  │  • license_id (FK → demo_licenses.id)                        │  │   │
│  │  │  • host (UNIQUE) - Azure DevOps Server hostname               │  │   │
│  │  │  • INDEX: idx_demo_license_host_license                       │  │   │
│  │  └──────────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                        INFRASTRUCTURE LAYER                                   │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Kubernetes Deployment                                              │   │
│  │  • Namespace: gotime                                                │   │
│  │  • Deployment: license-backend                                      │   │
│  │  • Service: ClusterIP (port 3001)                                   │   │
│  │  • Replicas: 1                                                      │   │
│  │  • Image: registry.higgscloud.com/rdc/.../license-backend:latest   │   │
│  │  • Health Probes: /healthz, /readyz                                 │   │
│  │  • Resources: 100m CPU, 256Mi Memory                                │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Database Infrastructure                                            │   │
│  │  • Production: MySQL (MariaDB)                                      │   │
│  │    - Helm chart: infrastructure/helm/mariadb/                       │   │
│  │    - Connection via environment variables                           │   │
│  │  • Development: SQLite (file-based)                                 │   │
│  │    - Location: backend/data/licenses.db                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Veri Akış Diyagramları

### 1. Demo Lisans Üretimi Akışı

```
┌─────────────┐
│   User      │
│ (Azure DevOps│
│   Admin)    │
└──────┬──────┘
       │
       │ 1. Opens Settings Panel
       │    (effort-settings.tsx)
       │
┌──────▼──────────────────────────────────┐
│   Frontend Extension                    │
│                                         │
│  User enters:                          │
│  • Organization: org1                  │
│  • Host: (optional)                    │
└──────┬─────────────────────────────────┘
       │
       │ 2. POST /api/license/demo
       │    {
       │      "allowedOrganizations": ["org1"],
       │      "allowedHosts": []
       │    }
       │
┌──────▼──────────────────────────────────┐
│   Backend Server                        │
│   (Express.js)                          │
└──────┬─────────────────────────────────┘
       │
       │ 3. Check for existing license
       │    (query demo_license_organizations)
       │
┌──────▼──────────────────────────────────┐
│   Database                              │
│                                         │
│  SELECT * FROM demo_license_organizations
│  WHERE organization = 'org1'
└──────┬─────────────────────────────────┘
       │
       │ 4. No existing license found
       │
┌──────▼──────────────────────────────────┐
│   License Generator Core                │
│                                         │
│  • Create payload (3 months, 100 users)
│  • Sign with RSA private key
│  • Generate license key
└──────┬─────────────────────────────────┘
       │
       │ 5. Store in database
       │
┌──────▼──────────────────────────────────┐
│   Database                              │
│                                         │
│  INSERT INTO demo_licenses
│  INSERT INTO demo_license_organizations
└──────┬─────────────────────────────────┘
       │
       │ 6. Return license key
       │
┌──────▼──────────────────────────────────┐
│   Backend Server                        │
│   Response: { licenseKey: "..." }       │
└──────┬─────────────────────────────────┘
       │
       │ 7. Display license key to user
       │
┌──────▼──────────────────────────────────┐
│   Frontend Extension                    │
│                                         │
│  User copies license key
│  User activates license
└──────┬─────────────────────────────────┘
       │
       │ 8. Save to Azure DevOps Settings
       │
┌──────▼──────────────────────────────────┐
│   Azure DevOps Collection Settings      │
│                                         │
│  licenseInfo: {                        │
│    licenseKey: "...",                  │
│    payload: {...},                     │
│    isValid: true                       │
│  }
└─────────────────────────────────────────┘
```

### 2. Lisans Doğrulama Akışı

```
┌─────────────────────────────────────┐
│   Extension Component (Any Page)    │
└──────────────┬──────────────────────┘
               │
               │ 1. LicenseManager.isLicensed()
               │
┌──────────────▼──────────────────────┐
│   LicenseManager                    │
│   (license-utils.ts)                │
└──────────────┬──────────────────────┘
               │
               │ 2. Get license from settings
               │
┌──────────────▼──────────────────────┐
│   Azure DevOps Collection Settings  │
│   licenseInfo: {...}                │
└──────────────┬──────────────────────┘
               │
               │ 3. Verify license
               │
┌──────────────▼──────────────────────┐
│   License Verification Steps        │
│                                     │
│  a. Parse license key (3 parts)    │
│  b. Extract payload, signature, PK │
│  c. Verify public key match        │
│  d. Verify RSA signature           │
│  e. Decode and validate payload    │
│  f. Check expiration date          │
│  g. Check organization/host match  │
│  h. Check user count (warning)     │
└──────────────┬──────────────────────┘
               │
               │ 4. Return result
               │
┌──────────────▼──────────────────────┐
│   Extension Component               │
│                                     │
│  if (isLicensed) {                 │
│    Show full features              │
│  } else {                          │
│    Show license required message   │
│  }
└─────────────────────────────────────┘
```

---

## 🔐 Güvenlik Mimarisi

### Lisans Güvenliği Katmanları

```
┌─────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                       │
└─────────────────────────────────────────────────────────┘

Layer 1: Transport Security
├── HTTPS (TLS/SSL)
└── Certificate validation

Layer 2: License Format Security
├── RSA Asymmetric Encryption
│   ├── Private Key (Backend only)
│   └── Public Key (Embedded in extension)
├── SHA-256 Hashing
└── Base64 Encoding

Layer 3: Signature Verification
├── Public key match check
├── RSA signature verification
└── Constant-time comparison (timing attack protection)

Layer 4: Payload Validation
├── Expiration date check
├── Organization/Host validation
├── User count validation
└── Required fields validation

Layer 5: Storage Security
├── Azure DevOps Collection Settings (encrypted at rest)
└── Database encryption (if configured)
```

### RSA Key Management

```
┌─────────────────────────────────────────────────────────┐
│              RSA KEY LIFECYCLE                          │
└─────────────────────────────────────────────────────────┘

Key Generation (tools/generate-keys.js)
│
├── Private Key
│   ├── Location: tools/keys/private.key
│   ├── Environment: LICENSE_PRIVATE_KEY
│   ├── Usage: License signing (backend only)
│   └── Security: NEVER exposed to client
│
└── Public Key
    ├── Location: tools/keys/public.key
    ├── Environment: LICENSE_PUBLIC_KEY
    ├── Embedded: Extension code (license-utils.ts)
    ├── Usage: Signature verification (client-side)
    └── Security: Can be public (part of license key)

License Key Structure:
┌─────────────┬──────────────┬──────────────┐
│   Payload   │  Signature   │  Public Key  │
│  (Base64)   │  (Base64)    │  (Base64)    │
└─────────────┴──────────────┴──────────────┘
     eyJ...      ABC123...      MII...
```

---

## 📊 Mevcut vs. Önerilen Mimari

### Mevcut Mimari (Current State)

```
Frontend Extension
    │
    │ API Calls (license generation only)
    │
Backend Server
    │
    │ Database Queries
    │
Database (demo_licenses only)
```

**Özellikler:**
- ✅ Demo lisans üretimi
- ✅ Lisans doğrulama (client-side)
- ❌ Kullanım takibi yok
- ❌ Müşteri yönetimi yok
- ❌ Raporlama yok
- ❌ Bildirim yok

### Önerilen Mimari (Recommended State)

```
┌─────────────────────────────────────────────────────────┐
│              Frontend Extension                         │
│  • License validation                                   │
│  • Usage tracking (NEW)                                 │
│  • Periodic heartbeat (NEW)                             │
└───────────────────────┬─────────────────────────────────┘
                        │
                        │ API Calls
                        │
┌───────────────────────▼─────────────────────────────────┐
│              Backend Server                             │
│  • License generation                                   │
│  • Usage tracking API (NEW)                             │
│  • Reporting API (NEW)                                  │
│  • Customer management API (NEW)                        │
└───────────┬───────────────────────┬─────────────────────┘
            │                       │
            │                       │
┌───────────▼───────────┐  ┌───────▼───────────┐
│    Database           │  │  Notification     │
│  • demo_licenses      │  │  Service (NEW)    │
│  • customers (NEW)    │  │  • Email          │
│  • licenses (NEW)     │  │  • Webhooks       │
│  • usage_logs (NEW)   │  │  • Scheduled jobs │
└───────────────────────┘  └───────────────────┘
            │
            │
┌───────────▼───────────┐
│  Admin Panel (NEW)    │
│  • React UI           │
│  • Dashboard          │
│  • Customer mgmt      │
│  • Reports            │
└───────────────────────┘
```

---

## 🚀 Deployment Topolojisi

### Kubernetes Deployment

```
┌─────────────────────────────────────────────────────────┐
│                  Kubernetes Cluster                      │
│                  (Namespace: gotime)                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  Deployment: license-backend                             │
│  ├── Replicas: 1                                        │
│  ├── Image: registry.higgscloud.com/.../license-backend │
│  ├── Port: 3001                                         │
│  └── Resources: 100m CPU, 256Mi Memory                  │
└─────────────────────────────────────────────────────────┘
                        │
                        │ Service: ClusterIP
                        │
┌───────────────────────▼─────────────────────────────────┐
│  Service: license-backend                                │
│  ├── Type: ClusterIP                                    │
│  ├── Port: 3001                                         │
│  └── Selector: app=license-backend                      │
└───────────────────────┬─────────────────────────────────┘
                        │
                        │ External Access (Ingress/LoadBalancer)
                        │
┌───────────────────────▼─────────────────────────────────┐
│  External URL: https://gotime.higgscloud.com            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  Secrets: license-backend-env                            │
│  ├── LICENSE_PRIVATE_KEY                                │
│  ├── LICENSE_PUBLIC_KEY                                 │
│  ├── MYSQL_HOST                                         │
│  ├── MYSQL_USER                                         │
│  ├── MYSQL_PASSWORD                                     │
│  └── MYSQL_DATABASE                                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  Database: MariaDB (Helm Chart)                          │
│  ├── Persistent Volume                                  │
│  ├── Backup Strategy                                    │
│  └── Connection Pooling                                 │
└─────────────────────────────────────────────────────────┘
```

---

## 📈 Ölçeklenebilirlik ve Performans

### Mevcut Durum
- **Backend**: Single instance (replica: 1)
- **Database**: Single MySQL instance
- **Load**: Düşük (sadece lisans üretimi)
- **Bottleneck**: Database connection pooling (10 connections)

### Önerilen İyileştirmeler

1. **Horizontal Scaling**
   - Backend replicas artırılabilir (stateless)
   - Load balancer gerekli

2. **Database Optimization**
   - Read replicas (reporting için)
   - Connection pool tuning
   - Query optimization
   - Indexing strategy

3. **Caching**
   - Redis cache (license validation results)
   - In-memory cache (frequently accessed licenses)

4. **Monitoring**
   - Application metrics (Prometheus)
   - Database metrics
   - API response times
   - Error rates

---

## 🔄 Entegrasyon Noktaları

### Azure DevOps Integration

```
Extension ←→ Azure DevOps APIs
├── Collection Settings (license storage)
├── Work Items API (time tracking)
├── Projects API (project listing)
└── Teams API (user count)
```

### External Services (Future)

```
Backend ←→ External Services
├── Email Service (SendGrid/Mailgun)
├── Webhook Service (customer notifications)
├── Analytics Service (usage analytics)
└── Payment Gateway (license purchases)
```

---

*Son Güncelleme: 2025-01-15*

